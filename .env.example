# LLM Provider Options:
# API-based: openai, anthropic, google
# CLI-based (use your subscription): gemini-cli, claude-cli, cursor-cli
DIFFLEARN_LLM_PROVIDER=gemini-cli

# API Keys (only needed for API-based providers)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=

# Optional: Custom model selection (for API providers)
DIFFLEARN_MODEL=gpt-4o

# Optional: Temperature (0.0-1.0)
DIFFLEARN_TEMPERATURE=0.3
